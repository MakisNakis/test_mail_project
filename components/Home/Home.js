import Mails from '../Inbox'

const Home = () => {
    return (
        <div className="h-full w-full bg-fill-secondary">
            <span className="text-font-quarternary">Выберите вкладку на панели слева</span>
        </div>
    )
}

export default Home
